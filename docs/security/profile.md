# Profile Security

Profile media uses a private S3 bucket with backend-generated presigned upload and read URLs. AWS credentials are server-side only.

## Controls

- All profile writes require authentication and CSRF header.
- Object keys are generated by the backend and scoped to the authenticated user.
- Confirm endpoints reject object keys outside the current user's avatar/banner prefix.
- SVG is rejected.
- Allowed types are `image/jpeg`, `image/png`, and `image/webp`.
- Avatar limit is 5 MB; banner limit is 10 MB.
- Public profile DTOs exclude email, password hash, OAuth IDs, tokens, auth state, and private account fields.
- `Private` and `FriendsOnly` profiles are hidden from non-owners in Module 2.

## Remaining Limitations

- `FriendsOnly` behaves like private until Module 3 implements friends.
- Production CloudFront delivery and deployed S3 verification remain planned.
