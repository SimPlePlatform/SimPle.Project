# Profile API Reference

Base path: `/api/profile`

All state-changing endpoints require `X-Requested-With: XMLHttpRequest`. Authentication uses HttpOnly cookies.

## Profile

- `GET /me` returns the authenticated user's profile.
- `PUT /me` updates display name, bio, region, status message, visibility, profile type, and legacy URL fields.
- `GET /{username}` returns a public profile when visibility allows it.
- `PUT /me/username` applies the monthly immediate username change when available, or creates/updates a pending admin-review request when the immediate monthly change was already used.
- `POST /me/username-change-request` stores a username-change request for review.
- `GET /me/username-change-request` returns the latest request.
- `PUT /me/username-change-request` edits the current pending request.
- `DELETE /me/username-change-request` cancels the current pending request without restoring the monthly request allowance.
- `GET /me/links` and `PUT /me/links` read/replace external links.
- `GET /me/interests` and `PUT /me/interests` read/replace interest tags.

Profile responses include `avatarUrl`, `bannerUrl`, `hasUploadedAvatar`, `hasUploadedBanner`, `color`, `initials`, `visibility`, `profileType`, and allowed external links. They do not include email, password hash, OAuth data, tokens, or private auth/security fields.

`profileType` is `Player` by default or `Developer` for game publisher/developer social identity. `Developer` does not grant admin, billing, subscription, or publishing permissions in Module 2.

`Gamer` was renamed to `Player`; stored legacy string values are migrated to `Player`.

## External Links

`GET /me/links` returns the owner-editable web profile links.

`PUT /me/links` replaces the authenticated user's links:

```json
{
  "links": [
    {
      "platform": "github",
      "url": "https://github.com/simple",
      "displayLabel": "Code",
      "sortOrder": 0
    }
  ]
}
```

Supported platforms are `github`, `xtwitter`/`twitter`, `instagram`, `discord`, and `website`. URLs must be valid absolute HTTPS URLs. `http`, `javascript:`, `data:`, `file:`, invalid, empty, duplicate platform+URL, and unsupported platform values are rejected. Public profile responses include links only when visibility allows the requester to view the profile.

## Avatar Upload

`POST /me/avatar/upload-url`

```json
{
  "fileName": "avatar.png",
  "contentType": "image/png",
  "fileSizeBytes": 123456
}
```

Returns a short-lived presigned PUT URL and backend-generated object key. The URL may point to local MinIO in development or AWS S3 in production; the frontend treats it as opaque.

```json
{
  "uploadUrl": "https://...",
  "objectKey": "profile-assets/users/{userId}/avatar/{uuid}.png",
  "contentType": "image/png",
  "expiresAtUtc": "2026-05-28T12:00:00Z"
}
```

Client uploads the file with `PUT uploadUrl`, then calls `POST /me/avatar/confirm`:

```json
{ "objectKey": "profile-assets/users/{userId}/avatar/{uuid}.png" }
```

Removal: `DELETE /me/avatar`

Fallback color: `PUT /me/avatar/fallback`

```json
{ "color": "#3366AA" }
```

## Banner Upload

`POST /me/banner/upload-url`

Same request shape as avatar. Object keys use:

```text
profile-assets/users/{userId}/banner/{uuid}.{ext}
```

Client uploads with `PUT uploadUrl`, then calls `POST /me/banner/confirm`.

Removal: `DELETE /me/banner`

Fallback color: `PUT /me/banner/fallback`

```json
{ "color": "#123456" }
```

## Media Validation

| Media | Max size | Allowed content types |
|---|---:|---|
| Avatar | 5 MB | `image/jpeg`, `image/png`, `image/webp` |
| Banner | 10 MB | `image/jpeg`, `image/png`, `image/webp` |

SVG is rejected. Object keys are generated by the backend and must belong to the authenticated user. Buckets stay private; display URLs are generated by the storage abstraction.

## Visibility

`Public` is visible to everyone. `Private` is owner-only. `FriendsOnly` is stored but behaves owner-only until Module 3 implements friends.

## Username Change Policy

Username changes are enforced server-side by UTC calendar month:

- 1 immediate username change per month.
- 1 admin-review username request per month after the immediate allowance is used.
- Editing a pending request updates the same request.
- Canceling a pending request marks it cancelled and does not restore the monthly request allowance.
- Approval changes the username later when admin review support is available.
